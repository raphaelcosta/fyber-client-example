<h1>Fyber Offers</h1>

<%= form_for :offers, url: search_offers_path do |f| %>
  <%= f.text_field :uid, placeholder: 'uid' %>
  <%= f.text_field :pub0, placeholder: 'pub0' %>
  <%= f.text_field :page, placeholder: 'page' %>
  <%= f.submit 'Get offers' %>
<% end %>
